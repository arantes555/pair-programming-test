const parseInput = (fileString) => {
  const state = []
  return state // TODO: implement
}

const iterate = (inputState) => {
  const outputState = []
  return outputState // TODO: implement
}

const formatState = (state) => {
  return `${state}` // TODO: implement
}

module.exports = { parseInput, iterate, formatState }
